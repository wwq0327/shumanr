<div class="posts">
  <h1 class="page-header-1"><a href="{{ post.get_absolute_url }}">{{ post.title }}</a></h1>
  <span class="info-border"><small>{{ post.pub_date|date:"Y/m/d" }} by <a href="{% url 'userena_profile_detail' post.author.username %}">{{ post.author }}</a></small></span>
  <div class="lesson-content" style="margin-top: 20px; margin-bottom: 40px;">
    {{ post.content|markdown:"safe" }}
  </div>
</div>
